# Corrections

`hooks/lifecycle/reflection-ask.sh` の Stop hook が尋ねた訂正・知見のうち、次回セッションへ残す価値があるものをここへ書く。`rules/` 直下に置くのは毎セッションの context へ自動で入るようにするためで、溜まったエントリは折を見て対象の規則ファイルへ統合し、統合済みの行はここから消す。

## エントリ

| 日付 | 訂正・知見 | 対象 |
| ---- | ---------- | ---- |
