# Corrections

`hooks/lifecycle/reflection-ask.sh` の Stop hook が尋ねた訂正・知見のうち、次回セッションへ残す価値があるものをここへ書く。溜まったエントリは折を見て対象の規則ファイルへ統合し、統合済みの行はここから消す。

## エントリ

| 訂正・知見                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 対象                                |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- |
| plan の `test_command` にシェルのメタ文字を書くと formatter が markdown の強調として解釈し、`test_command` を `test*command` に、`test-*.sh` を `test-[a-z]\*.sh` に書き換える。build の Load が読めなくなるので、メタ文字を含まない形で書く                                                                                                                                                                                                               | `skills/think/templates/plan.md`    |
| グローバル settings.json の hook はこのマシンで走る全 Claude Code プロセスで発火する。throttle の記録を 1 ファイルに置くと別プロセスと奪い合い、互いの ID で上書きし合って交互に発火する。記録は session ごとに分ける                                                                                                                                                                                                                                      | `hooks/lifecycle/reflection-ask.sh` |
| Finder で貼り替えたアプリアイコンはバンドル内に入るので、自動更新でバンドルが差し替わると消える。Ghostty は `macos-icon = custom` と `~/.config/ghostty/Ghostty.icns` で設定側へ移せる。変わるのは起動中の Dock アイコンだけで、バンドルを書き換えないため Finder と Launchpad は公式アイコンのまま残る                                                                                                                                                    | `~/.config/ghostty/config`          |
| `scout fetch` と `scout research` が返す Markdown は、コードブロック、表、リストを壊す。コード内も `< > * \ _ ~` をエスケープし、`<pre><code>` を二重にマークし、表の区切り行を出さない。`--raw` でも `--json` でも同じで抑止オプションは無い。コード内にバッククォートがあるとバックスラッシュを外しても復元できないので、GitHub 上のファイルなら `scout repo-read` を使う。原因は `fast_html2md 0.0.62` にあり、htmd への置き換えは thkt/scout#370       | `skills/use-cli-scout/SKILL.md`     |
| `scout fetch` は crates.io のクレートページを `--js` 付きでも status 404 で返す。クレートの最新バージョンは `https://docs.rs/crate/<name>/latest` か lib.rs から取る                                                                                                                                                                                                                                                                                       | `skills/use-cli-scout/SKILL.md`     |
| `scout repo-*` は GitHub API の `/repos/<owner>/<repo>` へ解決するので、GitLab のリポジトリには `error: Not found: /repos/<owner>/<repo>` を返す (`scout repo-overview Kanedias/html2md` で実測)。GitLab のファイルは `scout fetch https://gitlab.com/<owner>/<repo>/-/raw/<ref>/<path>` で取る。この経路では上の行のエスケープ回避策である `scout repo-read` が使えないので、`=\>` や `markup5ever\_rcdom` のバックスラッシュは手で外す                   | `skills/use-cli-scout/SKILL.md`     |
| `scout fetch` で docs.rs のソースビューア (`docs.rs/crate/<name>/<version>/source/<path>`) を取ると、本文の前に `[1](#1)` から始まる行番号リンクが全行分並ぶので、grep の行番号がファイルの行番号と一致しない。API ドキュメント側 (`docs.rs/<name>/latest/...`) にこの塊は出ない。行番号付きで引用するときは `scout repo-read` で GitHub 側を読み、その出力は `cat -n` 形式でファイルの行番号を埋め込むので、grep したら grep の番号でなく行内の番号を読む | `skills/use-cli-scout/SKILL.md`     |
| LLM への出力制約を文の数だけで書くと長さは縛れない。translate-tail の prompt は「2 文以内」しか課しておらず、300 字の 1 文がその制約を満たすので、PR の折りたたみ内は読めないまま残った。文数とは別に 1 文の字数上限を課す。ただし字数上限は file:line やパスを途中で切らせるので、逐語で保持する要素を含む文は上限の対象外だと prompt に明記する                                                                                                          | `workflows/build.js`                |
| `herdr plugin list` は sandbox 内では `PermissionDenied` を返して一覧を出さないので、`dangerouslyDisableSandbox` を付けて実行する。同じセッションで `herdr --help` と `herdr integration status` は sandbox 内で通ったので、herdr のサブコマンドが全部落ちるわけではない。代わりに `~/.config/herdr/plugins/config/` を ls しても、外した plugin の config ディレクトリ (`local.agentchat`) が残るので導入済み一覧にはならない                             | `~/.config/herdr/`                  |
